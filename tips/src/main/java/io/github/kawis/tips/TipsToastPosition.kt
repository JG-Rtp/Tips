package io.github.kawis.tips

/**
 * Position options for TipsToast.
 * Mirrors TipsDialogPosition but kept separate to avoid API confusion.
 */
enum class TipsToastPosition {
    TOP,
    CENTER,
    BOTTOM
}
